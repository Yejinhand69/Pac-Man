using UnityEngine;

public class SpawnNodes : MonoBehaviour
{
    [SerializeField] private int numToSpawn = 25;
    [SerializeField] private float currentSpawnOffset;
    [SerializeField] private float spawnOffset = 0.3f;

    void Start()
    {
        //nothing here for now
    }
    void Update()
    {
        //nothing here for now
    }
}
